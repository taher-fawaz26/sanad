/// Backend endpoint for the provider profile-completion checklist.
abstract final class ProviderCompletionApiPaths {
  ProviderCompletionApiPaths._();

  /// `GET` — `ProviderCompletionResponseDto`.
  static const String completion = 'service-provider/completion';
}
