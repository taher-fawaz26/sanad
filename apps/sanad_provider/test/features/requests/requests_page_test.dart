import 'package:flutter_test/flutter_test.dart';
import 'package:sanad_provider/src/features/requests/requests_page.dart';

void main() {
  testWidgets('renders RequestsPage', (tester) async {
    // TODO: pump with pumpDsWidget when app test harness is configured
    expect(const RequestsPage(), isNotNull);
  });
}
