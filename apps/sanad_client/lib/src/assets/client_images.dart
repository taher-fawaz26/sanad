/// App-specific raster images for `sanad_client`, under
/// `apps/sanad_client/assets/images/`.
///
/// Populate as `sanad_client` features gain real illustrations. Shared
/// illustrations (used by both apps) belong in `package:app_assets`
/// instead — never duplicate an asset that already exists there.
abstract final class ClientImages {
  ClientImages._();
}
