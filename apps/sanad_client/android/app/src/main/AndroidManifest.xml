<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Location — required by Google Maps SDK and location picker -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Biometric / device-credential unlock (local app lock).
         Also contributed by the local_auth plugin manifest; declared here
         so the app's full permission surface is auditable in one file. -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <!-- Android 13+ runtime notification permission. Requested through
         `Permissions.ensureNotifications()`, never by the messaging SDK. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Camera — required to attach a photo to an AI chat message -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Microphone — required to record a voice note and to run live voice -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Gallery / Photo access — required to attach images to a chat message -->
    <!-- READ_MEDIA_IMAGES is the API 33+ (Android 13+) replacement for READ_EXTERNAL_STORAGE -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <!-- READ_EXTERNAL_STORAGE is only needed on Android 12 and below -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />

    <!-- Neither is required to install: a device without them simply cannot
         reach the camera or voice affordances. -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />

    <application
        android:label="Sanad Client"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- Android App Links — one host per environment, since this
                 manifest is shared across all dart define ENV builds.
                 android:autoVerify requires a matching
                 /.well-known/assetlinks.json on each host (see
                 docs/DEEP_LINKING.md). -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="https" android:host="links.trysanad.us"/>
                <data android:scheme="https" android:host="stage-links.trysanad.us"/>
                <data android:scheme="https" android:host="qa-links.trysanad.us"/>
                <data android:scheme="https" android:host="dev-links.trysanad.us"/>
            </intent-filter>

            <!-- Custom-scheme fallback — works without domain verification,
                 useful for QA before assetlinks.json is published. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="sanadclient"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Speech-to-text in the chat composer. Android 11+ hides other
             packages by default, so without this the system recognizer is
             invisible and isRecognitionAvailable() silently returns false. -->
        <intent>
            <action android:name="android.speech.RecognitionService"/>
        </intent>
    </queries>
</manifest>
