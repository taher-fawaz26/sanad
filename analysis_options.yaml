# Root analysis_options.yaml — inherited by all packages via `include`.
# Each package may override specific rules in its own analysis_options.yaml.
include: package:flutter_lints/flutter.yaml

analyzer:
  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true
  errors:
    # Treat these as errors, not warnings
    missing_required_param: error
    missing_return: error
    dead_code: warning
    todo: info
  exclude:
    - "**/*.g.dart"
    - "**/*.freezed.dart"
    - "**/*.gen.dart"
    - "**/generated/**"
    - "**/gen/**"
    - "build/**"
    - ".dart_tool/**"

linter:
  rules:
    # Style
    prefer_single_quotes: true
    always_use_package_imports: true
    directives_ordering: true
    sort_pub_dependencies: false

    # Safety
    avoid_dynamic_calls: true
    avoid_returning_null_for_future: true
    cancel_subscriptions: true
    close_sinks: true

    # Design
    use_if_null_to_convert_nulls_to_bools: true
    avoid_positional_boolean_parameters: true
    avoid_bool_literals_in_conditional_expressions: true

    # Dead code
    avoid_unused_constructor_parameters: true

    # Documentation — disabled globally, enabled per package as needed
    public_member_api_docs: false
