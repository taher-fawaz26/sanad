name: change_password
description: Change password feature — authenticated password update flow.
publish_to: none
version: 0.1.0
resolution: workspace

environment:
  sdk: ">=3.11.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  equatable: ^2.1.0
  flutter_bloc: ^9.1.1
  fpdart: ^1.2.0
  go_router: ^17.3.0
  core:
    path: ../../core
  design_system:
    path: ../../design_system
  domain:
    path: ../../domain
  localization:
    path: ../../localization
  network:
    path: ../../network

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^9.0.0
  bloc_test: ^10.0.0
  mocktail: ^1.0.4
