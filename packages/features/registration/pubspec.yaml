name: registration
description: Sign-up flow — email entry, OTP verification, and account-type selection.
publish_to: none
version: 0.1.0

environment:
  sdk: ">=3.11.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  app_assets:
    path: ../../app_assets
  app_logger:
    path: ../../app_logger
  asset_picker:
    path: ../../asset_picker
  auth:
    path: ../auth
  core:
    path: ../../core
  design_system:
    path: ../../design_system
  dio: ^5.8.0+1
  easy_localization: ^3.0.8
  equatable: ^2.1.0
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  flutter_hooks: ^0.21.3+1
  flutter_svg: ^2.0.9
  fpdart: ^1.2.0
  go_router: ^17.3.0
  localization:
    path: ../../localization
  network:
    path: ../../network

resolution: workspace
