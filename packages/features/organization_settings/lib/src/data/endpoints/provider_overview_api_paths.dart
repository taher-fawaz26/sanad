/// Backend endpoint for the provider KPI-hub summary counts.
abstract final class ProviderOverviewApiPaths {
  ProviderOverviewApiPaths._();

  /// `GET` — `ServiceProviderOverviewResponseDto`.
  static const String overview = 'service-provider/overview';
}
