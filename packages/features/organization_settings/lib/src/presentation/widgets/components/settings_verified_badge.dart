import 'package:design_system/design_system.dart';
import 'package:flutter/material.dart';

/// Circular check badge shown next to verified contact field labels.
class SettingsVerifiedBadge extends StatelessWidget {
  /// Creates the verified badge using the theme primary color.
  const SettingsVerifiedBadge({super.key});

  @override
  Widget build(BuildContext context) => const AppVerifiedBadge();
}
