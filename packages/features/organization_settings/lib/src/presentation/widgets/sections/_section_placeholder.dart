import 'package:flutter/material.dart';

/// Temporary fixed-height slot so section scroll order can be verified.
///
/// Removed when each section gains its view-mode UI.
const Widget kSettingsSectionPlaceholder = SizedBox(height: 120);
