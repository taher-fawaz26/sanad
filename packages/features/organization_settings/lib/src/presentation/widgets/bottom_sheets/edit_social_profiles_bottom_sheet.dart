import 'package:flutter/material.dart';

/// Bottom sheet for editing organization social profile links.
class EditSocialProfilesBottomSheet extends StatelessWidget {
  /// Creates the edit social profiles bottom sheet placeholder.
  const EditSocialProfilesBottomSheet({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
