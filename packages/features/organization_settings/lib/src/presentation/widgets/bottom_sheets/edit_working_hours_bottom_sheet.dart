import 'package:flutter/material.dart';

/// Bottom sheet for editing organization working hours.
class EditWorkingHoursBottomSheet extends StatelessWidget {
  /// Creates the edit working hours bottom sheet placeholder.
  const EditWorkingHoursBottomSheet({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
