name: invitation
description: >
  Temporary UI-only invitation flow (details, OTP, success) — no backend
  integration; see InvitationModule doc comment.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ">=3.11.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  app_assets:
    path: ../../app_assets
  core:
    path: ../../core
  design_system:
    path: ../../design_system
  easy_localization: ^3.0.8
  equatable: ^2.1.0
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  go_router: ^17.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  testing:
    path: ../../testing
  very_good_analysis: ^9.0.0
