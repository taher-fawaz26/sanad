name: shared_models
description: Domain entities shared across both applications — User, Address, Service, Wallet, etc.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ">=3.11.0 <4.0.0"

dependencies:
  equatable: ^2.1.0
  fpdart: ^1.2.0
  core:
    path: ../core

dev_dependencies:
  very_good_analysis: ^9.0.0
  test: ^1.24.0
