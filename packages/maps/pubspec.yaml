name: maps
description: Shared Google Maps integration — reusable map widgets over google_maps_flutter.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ">=3.11.0 <4.0.0"

dependencies:
  core:
    path: ../core
  flutter:
    sdk: flutter
  fpdart: ^1.2.0
  geocoding: ^4.0.0
  geolocator: ^14.0.3
  google_maps_flutter: ^2.10.0
  permissions:
    path: ../permissions

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^9.0.0
