/// Sand Assets — centralized asset path constants for the Sand platform.
///
/// Every asset used by any app or package must be declared here.
/// Import this package to reference fonts, icons, images, animations,
/// lottie files, and illustrations without magic strings.
library;

// Asset paths will be generated here as the asset library grows.
// Example:
//   class SandImages { static const String logo = 'assets/images/logo.png'; }
//   class SandAnimations { static const String splash = 'assets/lottie/splash.json'; }
