name: storage
description: Local persistence — Hive encrypted cache, FlutterSecureStorage, SharedPreferences abstraction.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ">=3.11.0 <4.0.0"

dependencies:
  core:
    path: ../core
  flutter:
    sdk: flutter
  flutter_secure_storage: ^10.3.1
  hive_ce: ^2.19.3
  network:
    path: ../network
  path_provider: ^2.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^9.0.0
