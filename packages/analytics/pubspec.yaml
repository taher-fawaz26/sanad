name: analytics
description: Analytics — Firebase Analytics, event tracking, crash reporting via Sentry/Firebase Crashlytics.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ">=3.11.0 <4.0.0"

dependencies:
  core:
    path: ../core
  firebase_analytics: ^11.4.5
  firebase_core: ^3.15.2
  firebase_crashlytics: ^4.3.10
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^9.0.0
