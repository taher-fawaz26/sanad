## Unreleased

- Initial version: motion tokens (`AppMotionDuration`, `AppMotionCurve`), reduced-motion
  helper (`AppMotion`), reusable effects/patterns, page transitions
  (`AppPageTransitions`), and the centralized Lottie wrapper (`AppLottie`).
